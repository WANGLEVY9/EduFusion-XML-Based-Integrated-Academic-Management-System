# EduFusion 系统演示视频脚本

> 临时文档，录制完成后请删除。

---

## 第一部分：技术架构与项目实现（8-12 分钟）

### 1. 项目背景与目标（1-2 min）

- **一句话概括**：三个学院拥有异构数据库（SQL Server / Oracle / MySQL），需要一套统一的、基于 XML 的跨学院选课平台
- **核心痛点**：
  - 三套独立的数据库 schema，字段命名、数据类型完全不同
  - 学院间需要跨院选课、课程共享，但数据无法互通
  - 需要统一的接口规范，又不破坏各学院现有系统

### 2. 整体架构（3-4 min）

> 对照 README 中的 ASCII 架构图讲解

```
Client A ──→┐
Client B ──→┼──→ Integration Server ──→ College A Server ──→ SQL Server
Client C ──→┘                        ├──→ College B Server ──→ Oracle
                                     └──→ College C Server ──→ MySQL
```

- **三层架构**：
  - **表现层**（Client）：Swing GUI，各学院独立客户端
  - **业务层**（Integration Server）：请求路由、XSD 校验、XSLT 转换、结果聚合
  - **数据服务层**（College Server）：JDBC 直连各自数据库，以 HTTP/XML 暴露服务
- **数据流**：Client 发 XML → Integration Server 解析路由 → 转发 XML 给对应 College Server → College Server 转 JDBC → 结果逐级 XML 返回
- **设计模式**：`CollegeXmlHttpServer` 使用 **Composite 模式**统一包装所有 College 的 HTTP 接口，`CollegeGateway` 接口抽象了本地 JDBC 和远程 HTTP 两种实现

### 3. 技术亮点（3-4 min）

- **XML 技术栈**：
  - DOM4J 2.1.4 + Xerces 2.12.2 解析与序列化
  - XSD 校验（`request.xsd`）：6 种请求类型（shareCourse / crossSelect / dropCourse / statistics / queryCourses / myCourses）
  - XSLT 转换：10 个模板，解决三套数据库之间的字段名异构双向映射
- **异构数据库映射**（以学生姓名为例）：
  - College A（SQL Server）：`StudentA.sname`
  - College B（Oracle）：`StudentB.student_name`
  - College C（MySQL）：`StudentC.sname`
  - XSLT 在 XML 层完成双向转换，业务代码无感知
- **双模式运行**：
  - **Local 模式**：直连同一套 MySQL，方便开发调试
  - **Remote 模式**：走 XML/HTTP 调用远端 College Server，真实部署形态
  - 通过 `CollegeGateway` 接口 + `JdbcCollegeRepository` / `RemoteCollegeGateway` / `InMemoryCollegeStore` 三种实现切换

### 4. 模块结构（1-2 min）

| 模块 | 职责 | 端口 |
|------|------|------|
| `common` | 共享模型、XML 工具、XSD/XSLT、HTTP Server 基类 | - |
| `server-a` | College A 服务端（SQL Server 2022） | 8080 |
| `server-b` | College B 服务端（Oracle XE 21c） | 8081 |
| `server-c` | College C 服务端（MySQL 8.0） | 8082 |
| `integration-server` | 统一网关，请求路由与聚合 | 9090 |
| `client-a` | College A 客户端 GUI | - |
| `client-b` | College B 客户端 GUI | - |
| `client-c` | College C 客户端 GUI | - |

---

## 第二部分：系统运行与实操演示（10-15 分钟）

> **准备事项（录制前完成）**：
> - [ ] 三个数据库容器已运行（`docker ps` 确认）
> - [ ] 所有模块已编译（`mvn compile`）
> - [ ] Integration Server 已启动（端口 9090）
> - [ ] 三个 College Server 已启动（端口 8080/8081/8082）
> - [ ] 三个 Client 窗口位置已排好

### 1. 环境准备展示（1 min）

```
$ docker ps
CONTAINER ID   IMAGE                                  PORTS
...            mcr.microsoft.com/mssql/server:2022     1433→1433
...            gvenzl/oracle-xe:21c                    1521→1521
...            mysql:8.0                               3307→3306
```

```
$ mvn compile -q
[INFO] BUILD SUCCESS
```

### 2. 启动服务端（1 min 展示，可提前启动）

| 组件 | 命令 | 端口 |
|------|------|------|
| Integration Server | `mvn exec:java -pl integration-server` | 9090 |
| College A Server | `mvn exec:java -pl server-a` | 8080 |
| College B Server | `mvn exec:java -pl server-b` | 8081 |
| College C Server | `mvn exec:java -pl server-c` | 8082 |

### 3. 启动三个 Client 并登录（2 min）

```
# 三个独立终端窗口分别执行
mvn exec:java -pl client-a
mvn exec:java -pl client-b
mvn exec:java -pl client-c
```

| 学院 | 用户名 | 密码 |
|------|--------|------|
| A（SQL Server） | A001 | 123456 |
| B（Oracle） | B001 | 123456 |
| C（MySQL） | C001 | 123456 |

- 三个 GUI 窗口一字排开，同步展示
- 强调：**同一套代码库，三个不同后端数据库**

### 4. 功能演示（5-7 min）

#### 4.1 查看我的课程
- 三个学院分别点击「我的课程」按钮
- 验证：中文显示正常（如"高等数学""张教授"），各显示该学生已选课程

#### 4.2 查询所有课程
- 点击「查询课程」或「课程列表」
- 展示分页、按字段排序、按关键字筛选
- 可演示 CSV 导出功能

#### 4.3 跨院选课（核心功能）
- A001 搜索课程列表，找到 B 学院课程（如 B104 Web开发技术）
- 点击选课 → 弹出成功提示
- 再次查询「我的课程」→ 确认 B104 已出现在 A001 的课程列表中

#### 4.4 退课
- A001 退掉 B104 → 成功提示
- 验证「我的课程」中 B104 已移除

#### 4.5 共享课程
- 任意学院查看「共享课程列表」
- 展示各学院开放了哪些课程供跨院选修

#### 4.6 统计图表（选做）
- 点击「统计信息」
- JFreeChart 柱状图弹窗：展示各课程选课人数

### 5. 验证数据一致性（1 min）

- 在 College B 系统中查 A001 的选课记录（通过 B 的客户端查 A001 在 B 学院的课程）
- 说明跨院数据通过 XML 流转，实现了异构系统间的数据一致性

---

## 第三部分：总结与亮点回顾（3-5 分钟）

### 1. 与作业要求的吻合性

| 要求 | 实现情况 | 对应位置 |
|------|---------|---------|
| 三个异构数据库 | SQL Server 2022 / Oracle XE 21c / MySQL 8.0 | README 数据库章节 |
| 基于 XML 的数据交换 | 全链路 XML 格式（DOM4J + Xerces） | README XML API 章节 |
| XSD 校验 | `request.xsd` 6 种请求类型严格校验 | README XSD 文档章节 |
| XSLT 转换 | 10 个 XSLT 模板双向字段映射 | README XSLT 文档章节 |
| 跨院选课 | 首位字母路由（A/B/C）实现跨院选课、退课 | README 路由规则章节 |
| 统计图表 | JFreeChart 柱状图展示选课统计数据 | README Client 操作手册 |
| 图形化客户端 | Swing GUI，支持登录、查询、选课、退课、导出 | README Client 操作手册 |

### 2. 额外实现与技术优点

- **Composite 设计模式**：`CollegeXmlHttpServer` 统一所有学院接口，新增学院只需建新模块 + 数据库 + XSLT 模板，核心框架零改动
- **双模式设计**：Local 模式便于开发调试（单 MySQL 直连），Remote 模式是真实异构部署形态，通过配置切换
- **完善的错误处理体系**：
  - 统一错误码：`E001`~`E101`，覆盖参数错误、认证失败、数据库异常、内部错误
  - 审计日志（`audit.log`）和错误日志（`error.log`）分离记录
  - XSD 校验在入口层拦截格式错误请求
- **Docker 容器化**：
  - 三个数据库均有完整初始化脚本
  - SQL Server 处理了 NVARCHAR 中文支持
  - Oracle 使用 PL/SQL + EXECUTE IMMEDIATE
  - MySQL 指定 utf8mb4 字符集
- **Client GUI 用户体验细节**：
  - 表格支持点击列头排序
  - 支持关键字筛选
  - CSV 数据导出
  - 中文显示良好

### 3. 可扩展性

新增 College D 的步骤：
1. 建 `server-d` 模块 + 数据库初始化脚本
2. 添加 XSLT 模板做字段映射
3. Integration Server 配置中注册新学院
4. **核心框架无需任何修改**

---

## 附录：录制操作建议

### 窗口布局
```
┌─────────────────┬─────────────────┬─────────────────┐
│   Client A      │   Client B      │   Client C      │
│   (登录后)       │   (登录后)       │   (登录后)       │
│                 │                 │                 │
│  [我的课程]      │  [我的课程]      │  [我的课程]      │
│  [课程列表]      │  [课程列表]      │  [课程列表]      │
│  [共享课程]      │  [共享课程]      │  [共享课程]      │
│  [统计信息]      │  [统计信息]      │  [统计信息]      │
└─────────────────┴─────────────────┴─────────────────┘
```

### 录制节奏
- **第一部分**：可单独录制（讲解式），后期剪辑合成
- **第二部分**：建议一镜到底，展示流程连贯性
- **第三部分**：可对着镜头讲，也可配合 PPT/README 翻页

### 常见注意事项
- 终端字体调大至 16-18pt
- 关闭无关通知弹窗
- 鼠标操作不要太快，留 0.5-1 秒停顿方便后期剪辑
- Docker / Maven 命令可以提前运行好，录制时只用 `docker ps` 和 `ls` 展示状态
- 重点操作的鼠标点击处用 **画圈标注**（录屏软件功能）更好
